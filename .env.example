HOST=0.0.0.0
PORT=8000
# Comma-separated exact public Host header values for a deployment.
# App Platform uses ${APP_DOMAIN},${APP_DOMAIN}:* in .do/app.yaml.
# MCP_ALLOWED_HOSTS=mcp.example.org,mcp.example.org:*
