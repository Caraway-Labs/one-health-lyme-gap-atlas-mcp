name: oh-lyme-mcp-dev
region: sfo
alerts:
  - rule: DEPLOYMENT_FAILED
services:
  - name: mcp
    github:
      repo: Caraway-Labs/one-health-lyme-gap-atlas-mcp
      branch: main
      deploy_on_push: false
    dockerfile_path: Dockerfile
    http_port: 8000
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-0.5gb
    health_check:
      http_path: /healthz
      initial_delay_seconds: 20
      period_seconds: 15
      timeout_seconds: 5
      success_threshold: 1
      failure_threshold: 5
    envs:
      - key: HOST
        value: 0.0.0.0
        scope: RUN_TIME
      - key: PORT
        value: "8000"
        scope: RUN_TIME
      - key: MCP_ALLOWED_HOSTS
        value: "${APP_DOMAIN},${APP_DOMAIN}:*"
        scope: RUN_TIME
